Here's a breakdown of the task into subtasks and the resulting git patch.  Since the original HTML only included styling in the `<head>`, we'll need to add a `<button>` element.

**Subtasks:**

1. **Change background color to blue:** Modify the CSS to set the `background-color` of the `body` to blue.
2. **Change font size to 100%:**  Modify the CSS to set the `font-size` of the `html` element to `100%`. (Note:  Setting it to `100` is not valid; it needs a unit like `px`, `em`, or `%`.  `100%` is the standard default and likely what was intended.)
3. **Add a button named "Home" at the bottom of the page:** Add a `<button>` element with the text "Home" inside the `ui-footer` section of the HTML.  Style this button in the CSS.


**Git Patch:**

```diff
--- a/index.html
+++ b/index.html
@@ -18,6 +18,9 @@
 
 		<div role="main" class="ui-content">
 
+			<div data-role="footer" data-position="fixed" data-theme="b" id="myfooter">
+				<button>Home</button>
+			</div>
 			<form class="ui-filterable">
 				<input id="searchBar" data-type="search" placeholder="Filter books.." class="ui-focus">
 			</form>
@@ -70,6 +73,10 @@
 					});
 				</script>
 			</ul>
+		
 
 		</div><!-- /content -->
 
+		
+
 	</div><!-- /page -->
 </body>
 </html>
```

```diff
--- a/style.css
+++ b/style.css
@@ -1,3 +1,6 @@
+/* Body background */
+body {background-color: blue;}
+
 html {
     font-size: 100%
 }
@@ -1764,3 +1767,7 @@
 .ui-tabs {
     position: relative;
     padding: .2em
+}
+
+#myfooter button {
+	margin: 0.5em 0;
 }

```

This patch shows the changes made to the HTML and CSS files. To apply it, you would need to have the original `index.html` and `style.css` files in your project.  Then, you could use `git apply` (after creating a git repository).  Remember to adjust the CSS rules for the "Home" button as needed to match your desired visual style.  The provided CSS just adds some basic margin.
